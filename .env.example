# Security: This is the key users must send in the header (X-API-KEY)
API_KEY=your_super_secret_high_marks_key_2026

# Configuration: Define which Hugging Face model to load
MODEL_NAME=gpt2

# Server settings
PORT=8000
LOG_LEVEL=info